export interface ChartProps {
  currentChart: string;
  handleCurrentChart: (chart: string) => void;
  currentPair: string;
}

export interface ChartListProps {
  currentChart: string;
  handleCurrentChart: (chart: string) => void;
}
