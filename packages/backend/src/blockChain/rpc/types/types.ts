export interface ContractType {
  name: string;
  address: string;
}

export interface TokenContractType {
  name: string;
  address: string;
  // deployer: string;
}

export interface PairContractType {
  name: string;
  address: string;
  // deployer: string;
}
